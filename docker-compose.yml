services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: personal_context_os
    ports:
      - "15432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d personal_context_os"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    ports:
      - "16379:6379"
    volumes:
      - redis-data:/data

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123
    ports:
      - "19000:9000"
      - "19001:9001"
    volumes:
      - minio-data:/data

  api:
    build: .
    command: sh -c "pnpm --filter @personal-context-os/db migrate && pnpm --filter @personal-context-os/api dev"
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/personal_context_os
      REDIS_URL: redis://redis:6379
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: minio
      S3_SECRET_KEY: minio123
      S3_BUCKET: personal-context-os
      JWT_SECRET: change-me-in-production
      APP_BASE_URL: http://localhost:3000
      API_BASE_URL: http://localhost:4000
      MCP_SERVER_URL: http://localhost:4100/mcp
      BOOTSTRAP_USER_EMAIL: admin@me.com
      BOOTSTRAP_USER_NAME: Local User
      BOOTSTRAP_USER_PASSWORD: admin123
      SESSION_COOKIE_DOMAIN: ""
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_AUTH_MODE: ${OPENAI_AUTH_MODE:-api_key}
      OPENAI_API_BASE_URL: ${OPENAI_API_BASE_URL:-https://api.openai.com/v1}
      OPENAI_EXTRACTION_MODEL: ${OPENAI_EXTRACTION_MODEL:-gpt-4o-mini}
      OPENAI_EMBEDDING_MODEL: ${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}
      OPENAI_CODEX_BASE_URL: ${OPENAI_CODEX_BASE_URL:-https://chatgpt.com/backend-api/codex}
      OPENAI_CODEX_EXTRACTION_MODEL: ${OPENAI_CODEX_EXTRACTION_MODEL:-gpt-5.5}
      OPENAI_CODEX_ACCESS_TOKEN: ${OPENAI_CODEX_ACCESS_TOKEN:-}
      OPENAI_CODEX_REFRESH_TOKEN: ${OPENAI_CODEX_REFRESH_TOKEN:-}
      OPENAI_CODEX_ACCOUNT_ID: ${OPENAI_CODEX_ACCOUNT_ID:-}
      OPENAI_CODEX_TOKEN_EXPIRES_AT: ${OPENAI_CODEX_TOKEN_EXPIRES_AT:-}
      OPENAI_CODEX_TOKEN_ENCRYPTION_KEY: ${OPENAI_CODEX_TOKEN_ENCRYPTION_KEY:-}
      GOOGLE_OAUTH_CLIENT_ID: ""
      GOOGLE_OAUTH_CLIENT_SECRET: ""
      GOOGLE_CALENDAR_TOKEN_ENCRYPTION_KEY: ""
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      minio:
        condition: service_started

  worker:
    build: .
    command: pnpm --filter @personal-context-os/worker dev
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/personal_context_os
      REDIS_URL: redis://redis:6379
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: minio
      S3_SECRET_KEY: minio123
      S3_BUCKET: personal-context-os
      JWT_SECRET: change-me-in-production
      APP_BASE_URL: http://localhost:3000
      API_BASE_URL: http://api:4000
      MCP_SERVER_URL: http://localhost:4100/mcp
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_AUTH_MODE: ${OPENAI_AUTH_MODE:-api_key}
      OPENAI_API_BASE_URL: ${OPENAI_API_BASE_URL:-https://api.openai.com/v1}
      OPENAI_EMBEDDING_MODEL: ${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}
    depends_on:
      api:
        condition: service_started
      redis:
        condition: service_started

  mcp-server:
    build: .
    command: pnpm --filter @personal-context-os/mcp-server dev
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/personal_context_os
      REDIS_URL: redis://redis:6379
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: minio
      S3_SECRET_KEY: minio123
      S3_BUCKET: personal-context-os
      JWT_SECRET: change-me-in-production
      APP_BASE_URL: http://localhost:3000
      API_BASE_URL: http://api:4000
      MCP_SERVER_URL: http://localhost:4100/mcp
    ports:
      - "4100:4100"
    depends_on:
      api:
        condition: service_started

  web:
    build: .
    command: pnpm --filter @personal-context-os/web dev
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:4000
      API_BASE_URL: http://api:4000
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_started

volumes:
  postgres-data:
  redis-data:
  minio-data:
